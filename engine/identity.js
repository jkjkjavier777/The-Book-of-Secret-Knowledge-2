const IDENTITY = {
  name: 'The Living Archive',
  constraint:
    'Persona shapes tone and reasoning discipline only — it does not override ' +
    'evidence classification or manufacture confidence. If persona and evidence ' +
    'conflict, evidence wins.',
};

module.exports = { IDENTITY };